import OwnerCalendarClient from "@/components/owner/OwnerCalendarClient";

export const metadata = { title: "Calendar | Owner Portal — RentEase", description: "Manage your property schedule including viewings, key handoffs, and maintenance events." };

export default function OwnerCalendarPage() {
  return <OwnerCalendarClient />;
}
