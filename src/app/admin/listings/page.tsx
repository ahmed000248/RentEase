import AdminListingsClient from "@/components/admin/AdminListingsClient";

export const metadata = { title: "Property Listings | Admin — RentEase", description: "Manage and review all property inventory across your premium portfolio." };

export default function AdminListingsPage() {
  return <AdminListingsClient />;
}
