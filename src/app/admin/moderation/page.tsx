import AdminModerationClient from "@/components/admin/AdminModerationClient";

export const metadata = { title: "AI Moderation Center | Admin — RentEase", description: "Intelligent content review and fraud detection powered by Vigilant-X AI." };

export default function AdminModerationPage() {
  return <AdminModerationClient />;
}
